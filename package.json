{
    "name": "movies-app",
    "private": true,
    "version": "1.0.0",
    "packageManager": "pnpm@10.0.0",
    "scripts": {
        "dev": "pnpm -r --parallel dev",
        "build": "pnpm -r build",
        "lint": "pnpm -r lint",
        "test": "pnpm -r test",
        "cdk:synth": "cd packages/cdk && pnpm build && pnpm cdk:synth",
        "cdk:deploy": "cd packages/cdk && pnpm build && pnpm cdk:deploy",
        "deploy:remote": "AWS_PROFILE=movies-app aws ssm send-command --region us-east-1 --instance-ids i-070ace755c00bd065 --document-name AWS-RunShellScript --comment \"Deploy movies app\" --parameters commands='[\"sudo /srv/deploy.sh\"]'"
    },
    "devDependencies": {
        "typescript": "^5.9.3"
    },
    "dependencies": {
        "zod": "^4.1.13"
    }
}